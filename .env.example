# Clave de cifrado para desarrollo local (NO usar en produccion)
# Genera una con: node -e "require('fs').writeFileSync('.dev-key', require('crypto').randomBytes(32).toString('hex'))"
PROLOPE_AUTH_KEY=

# Entorno: "production" para activar HTTPS obligatorio y cookie Secure
NODE_ENV=
